serviceTree:
  landscape: &landscape pichu
  cluster: &cluster topaz

tags: &tags
  atomi.cloud/landscape: *landscape
  atomi.cloud/cluster: *cluster

auth:
  clientId:
    remoteName: PICHU_SULFOXIDE_SOS_CLIENT_ID
  clientSecret:
    remoteName: PICHU_SULFOXIDE_SOS_CLIENT_SECRET

vcluster:
  controlPlane:
    backingStore:
      etcd:
        deploy:
          statefulSet:
            persistence:
              volumeClaim:
                enabled: true
                storageClass: ebs-gp3
            highAvailability:
              replicas: 1
            annotations:
              <<: *tags
            labels:
              <<: *tags
            pods:
              labels:
                <<: *tags
              annotations:
                <<: *tags
    coredns:
      deployment:
        replicas: 1
        annotations:
          <<: *tags
        labels:
          <<: *tags
        pods:
          annotations:
            <<: *tags
          labels:
            <<: *tags
    ingress:
      host: pichu.topaz.kubernetes.atomi.cloud
    statefulSet:
      highAvailability:
        replicas: 1
      pods:
        labels:
          <<: *tags
        annotations:
          <<: *tags
      labels:
        <<: *tags
      annotations:
        <<: *tags
  exportKubeConfig:
    context: pichu-topaz
    server: https://pichu.topaz.kubernetes.atomi.cloud
